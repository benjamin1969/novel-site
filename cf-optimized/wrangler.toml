name = "novel-site-cf"
compatibility_date = "2026-04-20"
compatibility_flags = ["nodejs_compat"]

[build]
command = "npm run build"
output_directory = "out"  # 静态导出输出到out目录

[[routes]]
pattern = "/*"
zone_name = "ccwu.cc"
custom_domain = true